{{- if .Values.llmFast.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncratic-phase1.fullname" . }}-llm-fast
  labels:
    {{- include "syncratic-phase1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.llmFast.service.type }}
  selector:
    {{- include "syncratic-phase1.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: llm-fast
  ports:
    - name: http
      port: {{ .Values.llmFast.service.port }}
      targetPort: http
{{- end }}
