{{- if .Values.neo4j.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncratic-phase1.fullname" . }}-neo4j
  labels:
    {{- include "syncratic-phase1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.neo4j.service.type }}
  selector:
    {{- include "syncratic-phase1.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: neo4j
  ports:
    - name: http
      port: {{ .Values.neo4j.service.httpPort }}
      targetPort: http
    - name: bolt
      port: {{ .Values.neo4j.service.boltPort }}
      targetPort: bolt
{{- end }}
