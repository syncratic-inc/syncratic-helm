{{- if .Values.transformer.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncratic.fullname" . }}-transformer
  labels:
    {{- include "syncratic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.transformer.service.type }}
  selector:
    {{- include "syncratic.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: transformer
  ports:
    - name: http
      port: {{ .Values.transformer.service.port }}
      targetPort: http
{{- end }}
