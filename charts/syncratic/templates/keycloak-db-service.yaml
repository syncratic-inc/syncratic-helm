{{- if .Values.keycloak.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncratic.keycloakDbServiceName" . }}
  labels:
    {{- include "syncratic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.keycloak.database.service.type }}
  selector:
    {{- include "syncratic.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: keycloak-db
  ports:
    - name: postgres
      port: {{ .Values.keycloak.database.service.port }}
      targetPort: postgres
{{- end }}
