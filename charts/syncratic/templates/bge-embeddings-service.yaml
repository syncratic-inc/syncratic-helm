{{- if .Values.bgeEmbeddings.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncratic.fullname" . }}-bge-embeddings
  labels:
    {{- include "syncratic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.bgeEmbeddings.service.type }}
  selector:
    {{- include "syncratic.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: bge-embeddings
  ports:
    - name: http
      port: {{ .Values.bgeEmbeddings.service.port }}
      targetPort: http
{{- end }}
